---
phase: 02-module-based-licensing
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - supabase/functions/_shared/authorization.ts
  - src/pages/Admin/TenantList.tsx
  - src/pages/Admin/TenantModules.tsx
  - src/App.tsx
  - src/locales/en-US/common.json
  - src/locales/pt-BR/common.json
  - src/locales/es-ES/common.json
  - src/locales/fr-FR/common.json
autonomous: true
requirements:
  - P2-2
  - P2-5
must_haves:
  truths:
    - "Super admin can add/remove individual modules per tenant"
    - "Edge Functions verify module access via shared authorization"
  artifacts:
    - path: supabase/functions/_shared/authorization.ts
      provides: verifyModuleAccess(client, userId, tenantId, moduleId)
    - path: src/pages/Admin/TenantModules.tsx
      provides: Super admin UI to add/remove override modules for a tenant
    - path: src/App.tsx
      provides: Route /admin/tenants/:id/modules (super_admin only)
  key_links:
    - from: verifyModuleAccess
      to: has_tenant_access RPC and get_tenant_licensed_modules RPC
      via: service-role client
    - from: TenantModules
      to: tenant_licensed_modules table
      via: supabase.from('tenant_licensed_modules').insert/delete, source='override'
---

<objective>
Add Edge Function shared helper verifyModuleAccess() and super admin UI to add/remove override modules per tenant. Satisfies Phase 2 success criteria 2 and 5.

Purpose: (1) Every module-gated Edge Function can call verifyModuleAccess after authenticating and resolving tenantId. (2) Super admin can grant/revoke override modules so custom deals are possible without changing tier.

Output: authorization.ts extended with verifyModuleAccess; new page TenantModules with add/remove overrides; route and i18n.
</objective>

<execution_context>
@/Users/amacedo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amacedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-module-based-licensing/02-RESEARCH.md
@supabase/functions/_shared/authorization.ts
@src/pages/Admin/TenantList.tsx

# Existing patterns
- authorization.ts: callBooleanFunction(name, params, client) with client defaulting to createServiceRoleClient(). has_tenant_access takes _user_id, _tenant_id. get_tenant_licensed_modules takes p_tenant_id and returns text[].
- TenantList: useQuery ['admin','tenants'], select id,name,slug,status; table with rows; no subscription_tier_id or modules yet. Route: /admin/tenants with RoleGuard super_admin.
</context>

<tasks>

<task type="auto">
  <name>Task 1: verifyModuleAccess in _shared/authorization.ts</name>
  <files>supabase/functions/_shared/authorization.ts</files>
  <action>
Add export async function verifyModuleAccess(client: SupabaseClient, userId: string, tenantId: string, moduleId: string): Promise&lt;void&gt;. Implementation: (1) Call has_tenant_access with service-role client: callBooleanFunction('has_tenant_access', { _user_id: userId, _tenant_id: tenantId }, client). If false, throw new Error('Access denied to tenant'). (2) Call client.rpc('get_tenant_licensed_modules', { p_tenant_id: tenantId }). If error or !Array.isArray(data) or !data.includes(moduleId), throw new Error('Module not licensed'). Use same client (service role) for RPC so RLS does not block. Do not use authenticateRequest inside this function; caller passes userId and tenantId after auth.
  </action>
  <verify>
    <automated>grep -q "verifyModuleAccess\|get_tenant_licensed_modules\|has_tenant_access" supabase/functions/_shared/authorization.ts</automated>
  </verify>
  <done>Edge Functions can call verifyModuleAccess(supabase, userId, tenantId, moduleId) after auth; throws if no tenant access or module not licensed.</done>
</task>

<task type="auto">
  <name>Task 2: Super admin TenantModules page and route</name>
  <files>src/pages/Admin/TenantModules.tsx, src/App.tsx, src/pages/Admin/TenantList.tsx</files>
  <action>
1. Create TenantModules.tsx: Route param tenantId (from /admin/tenants/:id/modules). Fetch tenant (name, slug, subscription_tier_id) and list of override modules: from('tenant_licensed_modules').select('module_id').eq('tenant_id', tenantId).eq('source','override'). Also fetch license_modules for dropdown (id, name). Display tenant name; list of override module_ids with Remove button (delete from tenant_licensed_modules where tenant_id and module_id and source='override'). "Add module" control: select from license_modules not already in overrides; on submit insert into tenant_licensed_modules (tenant_id, module_id, source='override'). Use TanStack Query; after insert/delete invalidate ['tenant-licensed-modules', tenantId] and ['admin','tenant-modules', tenantId] so useLicensedModules and this page stay in sync. Protect with RoleGuard super_admin (or render inside existing admin layout that already gates by super_admin). Add i18n keys for title, add module, remove, etc. in all four locales (en-US, pt-BR, es-ES, fr-FR).
2. In TenantList.tsx: Add a column or row action "Modules" (link to /admin/tenants/{tenant.id}/modules) so super admin can open module management per tenant.
3. In App.tsx: Add route &lt;Route path="/admin/tenants/:id/modules" element={&lt;RoleGuard allowedRoles={["super_admin"]}&gt;&lt;TenantModules /&gt;&lt;/RoleGuard&gt;} /&gt; and lazy-load TenantModules next to TenantList.
  </action>
  <verify>
    <automated>grep -q "tenant_licensed_modules\|TenantModules\|admin/tenants/:id/modules" src/pages/Admin/TenantModules.tsx src/App.tsx</automated>
  </verify>
  <done>Super admin can open Modules from tenant list, add override (INSERT), remove override (DELETE); route protected; i18n in 4 languages.</done>
</task>

</tasks>

<verification>
- Manual: As super_admin, open tenant Modules page, add an override module, confirm it appears in list and in useLicensedModules() for that tenant; remove override and confirm it disappears.
- Edge Function: Any function that receives tenantId and moduleId can call verifyModuleAccess(createServiceRoleClient(), user.id, tenantId, moduleId) before proceeding.
</verification>

<success_criteria>
- verifyModuleAccess available in Edge Functions; super admin UI to add/remove tenant override modules; invalidate hook cache on mutation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-module-based-licensing/02-03-SUMMARY.md`
</output>
