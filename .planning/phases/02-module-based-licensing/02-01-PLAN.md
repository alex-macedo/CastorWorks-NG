---
phase: 02-module-based-licensing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260302000000_license_modules_tiers_tenant_licensed.sql
  - supabase/migrations/20260302000001_seed_license_modules_and_tiers.sql
autonomous: true
requirements:
  - P2-1
  - P2-2
must_haves:
  truths:
    - "Tenant tier determines which modules are available; switching tier changes access"
    - "Schema supports super admin add/remove of individual modules per tenant (tenant_licensed_modules)"
  artifacts:
    - path: supabase/migrations/20260302000000_license_modules_tiers_tenant_licensed.sql
      provides: license_modules, subscription_tiers, tier_modules, tenant_licensed_modules tables; RPC get_tenant_licensed_modules
    - path: supabase/migrations/20260302000001_seed_license_modules_and_tiers.sql
      provides: Seed data for ~25 modules, 7 tiers, tier_modules matrix from PROJECT.md
  key_links:
    - from: get_tenant_licensed_modules RPC
      to: tenants + tier_modules + tenant_licensed_modules
      via: tier modules UNION override rows (source='override')
      pattern: "STABLE SECURITY DEFINER"
---

<objective>
Add licensing schema and seed so tenant tier resolves to modules and super-admin overrides are stored. Enables useLicensedModules() and Edge Function checks in later plans.

Purpose: Phase 2 requires DB foundation — license_modules, subscription_tiers, tier_modules, tenant_licensed_modules, tenants.subscription_tier_id, and get_tenant_licensed_modules(p_tenant_id) RPC. All runtime checks use modules only; tiers are commercial bundles.

Output: Two migrations: (1) tables + ALTER tenants ADD subscription_tier_id + RPC; (2) seed. No frontend or Edge Function changes.
</objective>

<execution_context>
@/Users/amacedo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amacedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-module-based-licensing/02-RESEARCH.md

# Schema reference (PROJECT.md + RESEARCH.md)
- license_modules(id VARCHAR(50) PK, name, description, category, depends_on[], created_at)
- subscription_tiers(id VARCHAR(50) PK, name, price_monthly_brl, price_annual_brl, max_projects, max_users, max_storage_gb, trial_days, display_order, is_active)
- tier_modules(tier_id FK, module_id FK, PK(tier_id, module_id))
- tenant_licensed_modules(tenant_id FK, module_id FK, source 'tier'|'override', monthly_quota, current_usage, quota_resets_at, granted_at, expires_at, PK(tenant_id, module_id))
- tenants: add subscription_tier_id VARCHAR(50) REFERENCES subscription_tiers(id)
- RPC get_tenant_licensed_modules(p_tenant_id uuid) RETURNS text[] — (tier modules UNION tenant_licensed_modules WHERE source='override' AND (expires_at IS NULL OR expires_at > now())); SECURITY DEFINER; enforce has_tenant_access(auth.uid(), p_tenant_id) OR has_role(auth.uid(), 'super_admin').
</context>

<tasks>

<task type="auto">
  <name>Task 1: Licensing tables and get_tenant_licensed_modules RPC</name>
  <files>supabase/migrations/20260302000000_license_modules_tiers_tenant_licensed.sql</files>
  <action>
Create single migration file that:
1. CREATE TABLE license_modules (id VARCHAR(50) PRIMARY KEY, name VARCHAR(255) NOT NULL, description TEXT, category VARCHAR(50) NOT NULL, depends_on VARCHAR(50)[], created_at TIMESTAMPTZ DEFAULT NOW()).
2. CREATE TABLE subscription_tiers (id VARCHAR(50) PRIMARY KEY, name VARCHAR(255) NOT NULL, price_monthly_brl DECIMAL(10,2), price_annual_brl DECIMAL(10,2), max_projects INT, max_users INT, max_storage_gb INT, trial_days INT DEFAULT 0, display_order INT, is_active BOOLEAN DEFAULT true).
3. CREATE TABLE tier_modules (tier_id VARCHAR(50) REFERENCES subscription_tiers(id), module_id VARCHAR(50) REFERENCES license_modules(id), PRIMARY KEY (tier_id, module_id)).
4. CREATE TABLE tenant_licensed_modules (tenant_id UUID REFERENCES tenants(id) ON DELETE CASCADE, module_id VARCHAR(50) REFERENCES license_modules(id), source VARCHAR(20) DEFAULT 'tier' CHECK (source IN ('tier','override')), monthly_quota INT, current_usage INT DEFAULT 0, quota_resets_at TIMESTAMPTZ, granted_at TIMESTAMPTZ DEFAULT NOW(), expires_at TIMESTAMPTZ, PRIMARY KEY (tenant_id, module_id)).
5. ALTER TABLE public.tenants ADD COLUMN IF NOT EXISTS subscription_tier_id VARCHAR(50) REFERENCES public.subscription_tiers(id).
6. Enable RLS on the four new tables. Policies: license_modules, subscription_tiers, tier_modules — authenticated SELECT for all. tenant_licensed_modules: SELECT where has_tenant_access(auth.uid(), tenant_id) OR has_role(auth.uid(), 'super_admin'); INSERT/UPDATE/DELETE for super_admin only (or same-tenant admin if desired; RESEARCH: super_admin can add/remove).
7. CREATE FUNCTION get_tenant_licensed_modules(p_tenant_id UUID) RETURNS text[] LANGUAGE plpgsql STABLE SECURITY DEFINER SET search_path = public. Body: IF NOT (has_tenant_access(auth.uid(), p_tenant_id) OR has_role(auth.uid(), 'super_admin')) THEN RAISE EXCEPTION 'Access denied'; END IF; RETURN (SELECT COALESCE(array_agg(DISTINCT m.module_id ORDER BY m.module_id), '{}') FROM (SELECT tm.module_id FROM tenants t JOIN tier_modules tm ON tm.tier_id = t.subscription_tier_id WHERE t.id = p_tenant_id UNION SELECT tlm.module_id FROM tenant_licensed_modules tlm WHERE tlm.tenant_id = p_tenant_id AND tlm.source = 'override' AND (tlm.expires_at IS NULL OR tlm.expires_at > now())) m). GRANT EXECUTE TO authenticated.
Use BEGIN/COMMIT in migration.
  </action>
  <verify>
    <automated>Migration file exists; grep -q "get_tenant_licensed_modules\|tenant_licensed_modules\|tier_modules\|license_modules\|subscription_tiers" supabase/migrations/20260302000000_license_modules_tiers_tenant_licensed.sql</automated>
  </verify>
  <done>Four tables and RPC exist; RLS and GRANT applied. Caller must have tenant access or be super_admin.</done>
</task>

<task type="auto">
  <name>Task 2: Seed license_modules, subscription_tiers, tier_modules</name>
  <files>supabase/migrations/20260302000001_seed_license_modules_and_tiers.sql</files>
  <action>
Create data-only migration (run after 20260302000000). Seed from PROJECT.md Module Inventory and Tier-to-Module Matrix:
1. INSERT into license_modules: all ~25 module IDs with name, category (core, functional, portal, ai, enterprise). Use id, name, description, category; depends_on can be NULL or empty array for now.
2. INSERT into subscription_tiers: trial, sandbox, architect_office, architect_office_ai, construction, construction_ai, enterprise — with display_order and is_active true; price/max_* from PROJECT.md table optional for Phase 2.
3. INSERT into tier_modules: for each tier, insert (tier_id, module_id) for every module that has "x" in that column in PROJECT.md Tier-to-Module Matrix. E.g. trial gets all modules that have x in Trial column; sandbox gets core, financial_basic, schedule_basic, roadmap, mobile_app; etc.
Use ON CONFLICT DO NOTHING or equivalent so re-run is safe. Reference 02-RESEARCH.md and PROJECT.md tables for exact module IDs and tier names (Trial, Sandbox, Arch Office, Arch+AI, Construction, Constr+AI, Enterprise).
  </action>
  <verify>
    <automated>grep -c "INSERT INTO" supabase/migrations/20260302000001_seed_license_modules_and_tiers.sql | xargs -I{} test {} -ge 3</automated>
  </verify>
  <done>license_modules and subscription_tiers and tier_modules populated; tier-to-module matrix matches PROJECT.md.</done>
</task>

</tasks>

<verification>
- Run migrations in order on target DB (per AGENTS.md: scp + ssh docker exec to NG container). No automated DB test in repo; verify by: RPC get_tenant_licensed_modules(uuid) returns array when tenant has subscription_tier_id set and tier has tier_modules rows.
</verification>

<success_criteria>
- All four licensing tables exist with RLS. tenants has subscription_tier_id. get_tenant_licensed_modules enforces access and returns tier modules + override modules. Seed data matches PROJECT.md tier–module matrix.
</success_criteria>

<output>
After completion, create `.planning/phases/02-module-based-licensing/02-01-SUMMARY.md`
</output>
