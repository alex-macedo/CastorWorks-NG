---
phase: 00-new-supabase-db
task: 0
total_tasks: 7
status: planning_complete
last_updated: 2026-03-01T05:03:18.540Z
---

<current_state>

Phase 0 (New Supabase Database & Config/Template Migration) is fully **planned** but **not yet executed**. All planning artifacts for CastorWorks-NG live in this repo. No Supabase NG instance has been provisioned; no migrations have been run against an NG database; no config/template export/import has been performed. Next step is to provision the NG Supabase instance (e.g. new Docker container on Hostinger) and then follow the runbook.

</current_state>

<completed_work>

- **PROJECT.md** (`.planning/PROJECT.md`): Full project plan including identity, new Supabase DB section (config/template migration only), multi-tenancy architecture (shared schema + tenant_id), licensing (module-based, 7 tiers), AI strategy (Action credits, graceful degradation), 9 implementation phases (0–8), technical decisions, constraints, references, success metrics.
- **New Supabase database section**: Tables-to-copy list (config, templates, reference, AI, UI, optional); data-to-exclude list; export/import mechanics; SSH/container names (source: `supabase-db`, NG: `supabase-ng-db`); post-migration app config.
- **Runbook** (`docs/runbooks/ng-db-migration.md`): Step-by-step for AI agent: variables, schema apply (all migrations in order), export in dependency order (layer1, layer1b benchmarks, layer2, optional), pre-import FK drop for auth.users, import, post-import NULL created_by + re-add FKs, verify, app config. Explicit `pg_dump`/`psql` commands and table list. Schema reference to multi-tenancy-best-practices doc.
- **Multi-tenancy reference**: PROJECT.md and runbook both cite **CastorWorks/docs/plans/multi-tenant/multi-tenancy-best-practices.md** as authoritative reference for shared-schema-with-tenant_id schema design. References section added to PROJECT.md.
- **.gitignore**: Added `.migration-dumps` so dump files from the runbook are not committed.

</completed_work>

<remaining_work>

- **Phase 0 execution** (runbook): Provision new Supabase instance on Hostinger (e.g. container `supabase-ng-db`). Run all migrations from `supabase/migrations/` on NG DB. Export from current DB (layer1, layer1b, layer2, optional). Pre-import: drop FKs to auth.users if needed. Import dumps in order. Post-import: set created_by to NULL where needed, re-add FKs. Verify row counts and zero projects/clients. Set CastorWorks-NG `.env` to new Supabase URL and anon key.
- **Phases 1–8**: Not started (tenant infra, licensing, trial/subscription, AI metering, storage, edge security, super admin, launch prep).

</remaining_work>

<decisions_made>

- Use **new** Supabase database for CastorWorks-NG (not the current production DB); copy only schema + config/templates, no projects/clients/purchases.
- Follow **multi-tenancy-best-practices.md** (shared schema with tenant_id); do not use schema-per-tenant or DB-per-tenant.
- Runbook and schema design reference the doc in the **parent CastorWorks repo** (CastorWorks-NG was created without copying `docs/`).
- Export/import in **dependency order** (layer1 → layer1b benchmarks → layer2) to satisfy FKs; handle auth.users FKs by drop → import → NULL created_by → re-add.

</decisions_made>

<blockers>

- None. Provisioning the NG Supabase instance (new container on Hostinger) is an ops step that may require human or separate runbook.

</blockers>

<context>

CastorWorks-NG is the multi-tenant SaaS evolution of CastorWorks. This session produced the full planning surface (PROJECT.md), the DB migration runbook for config/templates only, and explicit linkage to the multi-tenancy best-practices document. All work is saved in the **CastorWorks-NG** repository. When resuming, either execute Phase 0 using the runbook (if NG instance exists) or start Phase 1 planning/execution (tenant tables, TenantContext, tenant-client) using PROJECT.md as the source of truth.

</context>

<next_action>

Start with: If the NG Supabase container is provisioned, run the runbook (`docs/runbooks/ng-db-migration.md`) Step 1 (apply migrations) then Steps 2–6 (export, pre-import, import, post-import, verify). If not provisioned, either provision it or begin Phase 1 (Foundation) by creating `tenants` and `tenant_users` migrations and TenantContext per PROJECT.md.

</next_action>
