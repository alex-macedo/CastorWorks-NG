---
phase: 05-billing-invoices
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/list-billing-history/index.ts
  - supabase/functions/get-invoice-pdf/index.ts
autonomous: true
requirements:
  - BILL-01
  - BILL-02
must_haves:
  truths:
    - "Backend can return billing history (invoices + payment attempt info) for the tenant from Stripe"
    - "Backend can generate and serve an invoice PDF for a tenant's Stripe invoice"
  artifacts:
    - path: supabase/functions/list-billing-history/index.ts
      provides: "Stripe-sourced billing history API (no DB cache)"
    - path: supabase/functions/get-invoice-pdf/index.ts
      provides: "Our own invoice PDF generation and download"
  key_links:
    - from: list-billing-history
      to: "subscriptions.stripe_customer_id"
      via: "Supabase query by tenant_id"
      pattern: "subscriptions.*stripe_customer_id"
    - from: list-billing-history
      to: "Stripe API"
      via: "stripe.invoices.list({ customer })"
      pattern: "invoices\\.list"
    - from: get-invoice-pdf
      to: "Stripe API"
      via: "stripe.invoices.retrieve + line items"
      pattern: "invoices\\.retrieve"
---

<objective>
Provide Edge Functions that (1) list billing history from Stripe for the tenant (invoices and payment attempt context) and (2) generate and serve our own invoice PDF for download. Data source is Stripe only; no database cache per user decision.
Purpose: Enable Billing tab to show history and "Download invoice" in-app.
Output: list-billing-history and get-invoice-pdf Edge Functions callable by the frontend.
</objective>

<execution_context>
@/Users/amacedo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amacedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-billing-invoices/05-CONTEXT.md
@supabase/functions/create-billing-portal-session/index.ts
@supabase/functions/_shared/authorization.ts
@src/hooks/useSubscription.ts
</context>

<interfaces>
From create-billing-portal-session: verifyTenantAccess(req, tenantId); subscriptions table .select('stripe_customer_id').eq('tenant_id', tenantId).maybeSingle().
From useSubscription: Subscription has stripe_customer_id, stripe_subscription_id (from DB). Edge Functions use service role and Stripe server-side only.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Edge Function list-billing-history</name>
  <files>supabase/functions/list-billing-history/index.ts</files>
  <action>
Create Edge Function list-billing-history. POST body: { tenant_id: string }. Use verifyTenantAccess(req, tenantId) from _shared/authorization.ts. Resolve stripe_customer_id from subscriptions (same pattern as create-billing-portal-session: .from('subscriptions').select('stripe_customer_id').eq('tenant_id', tenantId).not('stripe_customer_id','is',null).order('created_at',{ascending:false}).limit(1).maybeSingle()). If no customer, return 200 with { items: [] } (empty state). Call Stripe invoices.list({ customer: stripe_customer_id, limit: 100 }) with expand or fields as needed. Map each Stripe invoice to a minimal shape for the UI: id, number, date (created or period_end), amount_due, amount_paid, status, currency, attempt_count (Stripe invoice has attempt_count), paid_at if available. Return { items: Array } so the app can show "invoices + payment attempts" (each invoice row can show attempt info; no separate DB). Use Stripe SDK from esm.sh (same as create-billing-portal-session). CORS and JSON response pattern from create-billing-portal-session. Do not cache or write to DB — Stripe is single source of truth.
  </action>
  <verify>
    <automated>deno check supabase/functions/list-billing-history/index.ts 2>/dev/null || true; grep -q "invoices.list\|invoices\.list" supabase/functions/list-billing-history/index.ts</automated>
  </verify>
  <done>POST with valid tenant_id and Stripe customer returns 200 with items array; no customer returns 200 with items: [].</done>
</task>

<task type="auto">
  <name>Task 2: Edge Function get-invoice-pdf</name>
  <files>supabase/functions/get-invoice-pdf/index.ts</files>
  <action>
Create Edge Function get-invoice-pdf. POST body: { tenant_id: string, invoice_id: string }. Verify tenant with verifyTenantAccess(req, tenantId). Resolve stripe_customer_id from subscriptions (same as Task 1). Retrieve Stripe invoice: stripe.invoices.retrieve(invoice_id) and optionally stripe.invoices.retrieve(invoice_id, { expand: ['lines.data'] }). Verify invoice.customer === stripe_customer_id (or invoice.customer_id if expanded) so the invoice belongs to this tenant. Generate PDF using a Deno-compatible library (e.g. pdf-lib from esm.sh or jspdf): include invoice number, date, line items (description, amount), total, tenant/branding if desired (per Claude's discretion in CONTEXT). Return Response with body = PDF buffer, headers: Content-Type: application/pdf, Content-Disposition: attachment; filename="invoice-{number}.pdf". Do not redirect to Stripe hosted invoice URL — we generate our own PDF per user decision.
  </action>
  <verify>
    <automated>grep -q "invoices.retrieve\|invoices\.retrieve" supabase/functions/get-invoice-pdf/index.ts; grep -q "application/pdf" supabase/functions/get-invoice-pdf/index.ts</automated>
  </verify>
  <done>POST with valid tenant_id and invoice_id returns 200 with PDF body and attachment disposition; wrong customer returns 403/404.</done>
</task>

</tasks>

<verification>
- Invoke list-billing-history with tenant_id (with and without Stripe customer) and assert response shape and status.
- Invoke get-invoice-pdf with tenant_id and a known invoice id; assert PDF bytes and headers.
</verification>

<success_criteria>
- list-billing-history returns billing items from Stripe only; no DB reads for invoice data.
- get-invoice-pdf returns our own generated PDF for the given invoice when it belongs to the tenant.
</success_criteria>

<output>
After completion, create `.planning/phases/05-billing-invoices/05-01-SUMMARY.md`
</output>
