---
phase: 02-module-based-licensing
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/hooks/useLicensedModules.ts
  - src/components/ModuleGuard.tsx
  - src/components/UpgradePrompt.tsx
  - src/constants/rolePermissions.ts
  - src/components/Layout/AppSidebar.tsx
  - src/locales/en-US/common.json
  - src/locales/pt-BR/common.json
  - src/locales/es-ES/common.json
  - src/locales/fr-FR/common.json
autonomous: true
requirements:
  - P2-3
  - P2-4
must_haves:
  truths:
    - "useLicensedModules() and ModuleGuard enforce access in the app"
    - "Sidebar options filter by required_module and tenant license"
  artifacts:
    - path: src/hooks/useLicensedModules.ts
      provides: modules array, hasModule(id), isLoading; calls get_tenant_licensed_modules RPC
    - path: src/components/ModuleGuard.tsx
      provides: Gate by module; fallback or children
    - path: src/components/UpgradePrompt.tsx
      provides: Placeholder upsell/upgrade UI (i18n)
    - path: src/constants/rolePermissions.ts
      provides: required_module on SidebarOptionConfig and SIDEBAR_OPTIONS entries
    - path: src/components/Layout/AppSidebar.tsx
      provides: Filter options by !option.required_module || hasModule(option.required_module)
  key_links:
    - from: useLicensedModules
      to: get_tenant_licensed_modules RPC
      via: supabase.rpc('get_tenant_licensed_modules', { p_tenant_id: tenantId })
    - from: AppSidebar
      to: useLicensedModules
      via: hasModule(option.required_module) in filter
---

<objective>
Implement frontend licensing: useLicensedModules() hook, ModuleGuard, UpgradePrompt, and sidebar filtering by required_module so that tier and overrides drive what the user can see and access.

Purpose: Phase 2 success criteria 3 and 4 — runtime enforcement in the app and sidebar filtered by module. Depends on 02-01 (RPC and schema).

Output: New hook, two components, extended SidebarOptionConfig and AppSidebar filter, i18n for UpgradePrompt (and any new keys).
</objective>

<execution_context>
@/Users/amacedo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amacedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-module-based-licensing/02-RESEARCH.md
@src/contexts/TenantContext.tsx
@src/constants/rolePermissions.ts
@src/components/Layout/AppSidebar.tsx

# Key interfaces (from codebase)
- useTenant() returns { tenantId, tenants, loading, setTenantId, refreshTenants }. useLicensedModules must run inside TenantProvider and use tenantId.
- SidebarOptionConfig: id, titleKey?, title?, type, icon, path?, tabs, allowedRoles[]. Add optional required_module?: string.
- AppSidebar uses useSidebarPermissions() and filters SIDEBAR_OPTIONS by hasOptionAccess(option.id); add condition (!option.required_module || hasModule(option.required_module)).
</context>

<tasks>

<task type="auto">
  <name>Task 1: useLicensedModules hook and ModuleGuard + UpgradePrompt</name>
  <files>src/hooks/useLicensedModules.ts, src/components/ModuleGuard.tsx, src/components/UpgradePrompt.tsx</files>
  <action>
1. Create useLicensedModules.ts: Use useTenant() for tenantId. useQuery key ['tenant-licensed-modules', tenantId], queryFn: if !tenantId return []; else supabase.rpc('get_tenant_licensed_modules', { p_tenant_id: tenantId }), map (data ?? []) as string[]. enabled: !!tenantId, staleTime 2 min. Return { modules: data ?? [], hasModule: (id: string) => modules.includes(id), isLoading }. Use useCallback for hasModule. Use supabase from @/integrations/supabase/client (tenant-client or main client; ensure set_tenant_context is called by TenantProvider so RPC runs in tenant context if needed — RPC takes p_tenant_id explicitly so anon client is fine).
2. Create ModuleGuard.tsx: Props { module: string, fallback: ReactNode, children: ReactNode }. Use useLicensedModules(); if isLoading render spinner (use existing Skeleton or Loader2 from ui); if !hasModule(module) return fallback; else return children.
3. Create UpgradePrompt.tsx: Props { module?: string }. Simple placeholder: message that this feature requires a higher plan, with optional module label. Use i18n key e.g. licensing.upgradePrompt (namespace licensing or common). Add keys to all four locale files (en-US, pt-BR, es-ES, fr-FR). No tier comparison link required for Phase 2 (can be Phase 3).
  </action>
  <verify>
    <automated>npm run lint -- --max-warnings 0 2>/dev/null | head -5; npm run validate:json 2>/dev/null | tail -3</automated>
  </verify>
  <done>Hook returns modules and hasModule; ModuleGuard gates by module; UpgradePrompt has i18n in 4 languages.</done>
</task>

<task type="auto">
  <name>Task 2: Sidebar required_module and filter</name>
  <files>src/constants/rolePermissions.ts, src/components/Layout/AppSidebar.tsx</files>
  <action>
1. In rolePermissions.ts: Add to SidebarOptionConfig type optional required_module?: string. For each SIDEBAR_OPTIONS entry that maps to a licensed module (per RESEARCH/PROJECT.md), set required_module. Examples: financials → 'financial_full'; castormind-ai → 'ai_core'; templates → 'templates'; architect section → 'architect_portal'; team-workspace campaigns → 'campaigns'; forms → 'forms'; reports → 'reports'; content-hub → 'content_hub'. Options with no required_module remain visible when user has role access only. Use PROJECT.md module IDs exactly (e.g. financial_full, ai_core, templates, architect_portal, client_portal, campaigns, forms, reports, content_hub).
2. In AppSidebar.tsx: Import useLicensedModules. Where filtered options are built (database permissions path and fallback path), add module check: keep option only if (!option.required_module || hasModule(option.required_module)) in addition to existing role/tab checks. If useLicensedModules is loading, either hide options with required_module or treat as no module (conservative: hide module-gated options until loaded).
  </action>
  <verify>
    <automated>grep -q "required_module\|useLicensedModules\|hasModule" src/constants/rolePermissions.ts src/components/Layout/AppSidebar.tsx</automated>
  </verify>
  <done>SidebarOptionConfig has required_module; key sidebar options have it set; AppSidebar filters by hasModule(option.required_module).</done>
</task>

</tasks>

<verification>
- Manual: Switch tenant (or tier) and confirm sidebar items appear/disappear per tier. ModuleGuard wraps a route and shows UpgradePrompt when tenant lacks module.
</verification>

<success_criteria>
- useLicensedModules() returns effective modules for current tenant; ModuleGuard and UpgradePrompt work; sidebar hides options whose required_module is not licensed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-module-based-licensing/02-02-SUMMARY.md`
</output>
