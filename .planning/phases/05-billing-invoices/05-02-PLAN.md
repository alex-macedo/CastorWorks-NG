---
phase: 05-billing-invoices
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/pages/Settings.tsx
  - src/components/Settings/BillingPage.tsx
  - src/hooks/useBillingHistory.ts
  - src/locales/en-US/settings.json
  - src/locales/pt-BR/settings.json
  - src/locales/es-ES/settings.json
  - src/locales/fr-FR/settings.json
autonomous: true
requirements:
  - BILL-01
  - BILL-02
must_haves:
  truths:
    - "User can view billing history (invoices and payment attempt info) in a dedicated Billing tab in Settings"
    - "User sees empty state 'No billing history yet' when there are no items"
    - "User can download an invoice as our-generated PDF via 'Download invoice' in the app"
  artifacts:
    - path: src/pages/Settings.tsx
      provides: "Billing tab (separate from Subscription tab per user decision)"
    - path: src/components/Settings/BillingPage.tsx
      provides: "Billing history list and download actions"
    - path: src/hooks/useBillingHistory.ts
      provides: "Data from list-billing-history Edge Function"
  key_links:
    - from: src/pages/Settings.tsx
      to: "Billing tab content"
      via: "TabsTrigger value=billing, TabsContent with BillingPage"
      pattern: "billing|BillingPage"
    - from: src/components/Settings/BillingPage.tsx
      to: "list-billing-history"
      via: "useBillingHistory invokes Edge Function"
      pattern: "list-billing-history|useBillingHistory"
    - from: src/components/Settings/BillingPage.tsx
      to: "get-invoice-pdf"
      via: "Download button calls Edge Function and triggers blob download"
      pattern: "get-invoice-pdf|invoke.*invoice.*pdf"
---

<objective>
Add a separate "Billing" tab in Settings (not on the Subscription page) that shows billing history from Stripe and allows downloading our-generated invoice PDFs. Per user decision: Billing tab is distinct; empty state when no history; we offer "Download invoice" in-app using our PDF.
Purpose: Satisfy BILL-01 (view billing history) and BILL-02 (obtain invoices) via UI.
Output: Billing tab, BillingPage component, useBillingHistory hook, and billing i18n in all four locales.
</objective>

<execution_context>
@/Users/amacedo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amacedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-billing-invoices/05-CONTEXT.md
@src/pages/Settings.tsx
@src/components/Settings/SubscriptionPage.tsx
@src/hooks/useSubscription.ts
</context>

<interfaces>
Settings uses TabsTrigger value="subscription" and TabsContent value="subscription" with SubscriptionPage. Add TabsTrigger value="billing" (after subscription) and TabsContent with BillingPage. useSubscription provides tenant context; Edge Functions are invoked via supabase.functions.invoke('list-billing-history', { body: { tenant_id } }) and similarly for get-invoice-pdf with { tenant_id, invoice_id }. Auth: use session/token for Edge Function Authorization header (existing pattern).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Billing tab and BillingPage shell</name>
  <files>src/pages/Settings.tsx, src/components/Settings/BillingPage.tsx, src/locales/en-US/settings.json, src/locales/pt-BR/settings.json, src/locales/es-ES/settings.json, src/locales/fr-FR/settings.json</files>
  <action>
Add a "Billing" tab to Settings: (1) In Settings.tsx add TabsTrigger value="billing" with label from t('settings:tabs.billing') placed after the subscription tab (same TabsList). Add TabsContent value="billing" that renders <BillingPage />. (2) Create src/components/Settings/BillingPage.tsx as a Card-based layout (same pattern as SubscriptionPage): one Card for "Billing history" with a placeholder or "Loading..." for now. (3) Add i18n key settings.tabs.billing = "Billing" (en-US), and equivalent in pt-BR, es-ES, fr-FR. Do not add billing content to SubscriptionPage — Billing is a separate tab per user decision.
  </action>
  <verify>
    <automated>grep -q "value=\"billing\"\|value='billing'" src/pages/Settings.tsx; grep -q "BillingPage" src/pages/Settings.tsx; test -f src/components/Settings/BillingPage.tsx</automated>
  </verify>
  <done>Settings shows Billing tab; clicking it shows BillingPage with a card. All four locale files contain tabs.billing.</done>
</task>

<task type="auto">
  <name>Task 2: useBillingHistory hook and billing history list with empty state</name>
  <files>src/hooks/useBillingHistory.ts, src/components/Settings/BillingPage.tsx, src/locales/en-US/settings.json, src/locales/pt-BR/settings.json, src/locales/es-ES/settings.json, src/locales/fr-FR/settings.json</files>
  <action>
Create useBillingHistory hook: useTenantId(), then useQuery that calls supabase.functions.invoke('list-billing-history', { body: { tenant_id } }) with Authorization from session; return { items, isLoading, error } from the response body (items array from 05-01). In BillingPage, use useBillingHistory(); render a table or card list of items (date, number, amount, status, attempt_count if present). When items is empty or undefined and not loading, show empty state with message exactly "No billing history yet" (use i18n key e.g. settings:billing.emptyState) so the billing section is always visible per user decision. Add billing.emptyState (and any other billing.* keys used) to all four locale files (en-US, pt-BR, es-ES, fr-FR).
  </action>
  <verify>
    <automated>grep -l "list-billing-history" src/hooks/useBillingHistory.ts; grep -q "No billing history yet\|billing.emptyState\|emptyState" src/components/Settings/BillingPage.tsx</automated>
  </verify>
  <done>Billing tab shows list of billing items when available; shows "No billing history yet" when items are empty.</done>
</task>

<task type="auto">
  <name>Task 3: Download invoice button and PDF download</name>
  <files>src/components/Settings/BillingPage.tsx, src/locales/en-US/settings.json, src/locales/pt-BR/settings.json, src/locales/es-ES/settings.json, src/locales/fr-FR/settings.json</files>
  <action>
In BillingPage, for each invoice row (or card) that has an invoice id and is suitable for download (e.g. status paid or finalized), add a "Download invoice" button. On click: get session for Authorization, call supabase.functions.invoke('get-invoice-pdf', { body: { tenant_id, invoice_id } }) and receive blob/arrayBuffer; create a blob URL and trigger download (e.g. create object URL, programmatic &lt;a download&gt; click, filename invoice-{number}.pdf). Use i18n for button label (e.g. settings:billing.downloadInvoice) in all four locales. Handle errors (e.g. 403/404) with toast or inline message.
  </action>
  <verify>
    <automated>grep -q "get-invoice-pdf\|downloadInvoice\|download.*invoice" src/components/Settings/BillingPage.tsx</automated>
  </verify>
  <done>User can click "Download invoice" and receive our-generated PDF file for that invoice.</done>
</task>

</tasks>

<verification>
- Open Settings → Billing tab: see billing history or "No billing history yet".
- For a tenant with Stripe invoices, each invoice row has Download invoice; clicking it downloads a PDF.
</verification>

<success_criteria>
- Billing tab is separate from Subscription tab. Billing history shows invoices and payment attempt context. Empty state shown when no history. Download invoice delivers our PDF.
</success_criteria>

<output>
After completion, create `.planning/phases/05-billing-invoices/05-02-SUMMARY.md`
</output>