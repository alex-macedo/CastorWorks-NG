---
phase: 07-ai-action-credits-metering
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260307000000_ai_usage_log_and_credits.sql
  - src/hooks/__tests__/useAIUsage.test.ts
  - src/components/Settings/__tests__/AIUsagePage.test.tsx
  - supabase/functions/__tests__/ai-metering.test.ts
autonomous: true
requirements:
  - AI-01
  - AI-02
  - AI-03

must_haves:
  truths:
    - "ai_usage_log table exists with tenant_id, user_id, feature, actions_consumed, model_used, tokens, cost, cached, created_at"
    - "subscription_tiers table has ai_monthly_credits column seeded: Trial=100, Arch+AI=500, Constr+AI=2000, Enterprise=NULL"
    - "tenants table has ai_credits_purchased INT DEFAULT 0 column"
    - "consume_ai_actions Postgres RPC atomically debits credits and returns { allowed, degraded, remaining }"
    - "add_ai_credits RPC atomically increments ai_credits_purchased on a tenant"
    - "Wave 0 test scaffolds exist for useAIUsage, AIUsagePage, and ai-metering Deno tests"
  artifacts:
    - path: "supabase/migrations/20260307000000_ai_usage_log_and_credits.sql"
      provides: "All schema changes for Phase 7 in a single migration"
      contains: "consume_ai_actions"
    - path: "src/hooks/__tests__/useAIUsage.test.ts"
      provides: "Wave 0 test scaffold for hook (AI-01, AI-02, AI-03)"
    - path: "src/components/Settings/__tests__/AIUsagePage.test.tsx"
      provides: "Wave 0 test scaffold for UI states (AI-04)"
    - path: "supabase/functions/__tests__/ai-metering.test.ts"
      provides: "Wave 0 Deno test scaffold for model routing (AI-04)"
  key_links:
    - from: "consume_ai_actions RPC"
      to: "ai_usage_log table"
      via: "INSERT inside SECURITY DEFINER function"
      pattern: "INSERT INTO public\\.ai_usage_log"
    - from: "consume_ai_actions RPC"
      to: "subscription_tiers.ai_monthly_credits"
      via: "SELECT to get tier budget"
      pattern: "SELECT ai_monthly_credits"
    - from: "add_ai_credits RPC"
      to: "tenants.ai_credits_purchased"
      via: "atomic UPDATE with addition"
      pattern: "ai_credits_purchased.*\\+"
---

<objective>
Lay the complete database foundation for AI metering and create Wave 0 test scaffolds that later tasks implement against.

Purpose: Every downstream task (Plan 02 EF wiring, Plan 03 frontend) depends on this schema and these test files existing. The Postgres RPCs provide the atomicity guarantee that prevents credit race conditions.
Output: One migration file with all schema changes + three test scaffold files (failing — Wave 0).
</objective>

<execution_context>
@/Users/amacedo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amacedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-action-credits-metering/07-CONTEXT.md
@.planning/phases/07-ai-action-credits-metering/07-RESEARCH.md
@.planning/phases/07-ai-action-credits-metering/07-VALIDATION.md

<interfaces>
<!-- Key schema context for the executor. -->

Confirmed tier IDs from seed migration 20260302000001:
  trial, sandbox, architect_office, architect_office_ai,
  construction, construction_ai, enterprise

From supabase/functions/_shared/authorization.ts:
  export const createServiceRoleClient = () => createClient(url, serviceKey)
  export async function authenticateRequest(req): Promise<{ user: User; token: string }>
  export async function verifyTenantAccess(userId, tenantId, client?)

From src/hooks/useSubscription.ts (reference pattern for useAIUsage):
  export function useSubscription(): UseSubscriptionResult
  useQuery({ queryKey: ['tenant', tenantId, 'subscription'], queryFn, enabled: !!tenantId })

Existing test pattern reference:
  supabase/functions/__tests__/analyze-site-photos.test.ts (Deno, TEST_MODE=stub)
  Vitest config embedded in vite.config.ts; run with npm run test:run
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Database migration — ai_usage_log table, schema columns, and atomic RPCs</name>
  <files>supabase/migrations/20260307000000_ai_usage_log_and_credits.sql</files>
  <behavior>
    - consume_ai_actions('enterprise-tenant-id', 'ai-chat', 1, user_id, 'anthropic') returns { allowed: true, degraded: false, remaining: 999999 }
    - consume_ai_actions for a tenant with 80 used / 100 budget returns { allowed: true, degraded: false, remaining: 20 }
    - consume_ai_actions for a tenant with 0 remaining returns { allowed: true, degraded: true, remaining: 0 }
    - add_ai_credits increments ai_credits_purchased atomically (no race window)
  </behavior>
  <action>
Create `supabase/migrations/20260307000000_ai_usage_log_and_credits.sql` with all of the following in order:

1. CREATE TABLE public.ai_usage_log:
   - id UUID PRIMARY KEY DEFAULT gen_random_uuid()
   - tenant_id UUID NOT NULL REFERENCES public.tenants(id) ON DELETE CASCADE
   - user_id UUID REFERENCES auth.users(id) ON DELETE SET NULL
   - feature TEXT NOT NULL
   - actions_consumed INT NOT NULL DEFAULT 1
   - model_used TEXT
   - actual_tokens_in INT DEFAULT 0
   - actual_tokens_out INT DEFAULT 0
   - actual_cost_brl DECIMAL(10,6) DEFAULT 0
   - cached BOOLEAN DEFAULT FALSE
   - created_at TIMESTAMPTZ DEFAULT NOW()
   Use IF NOT EXISTS. Do NOT touch the old ai_usage_logs table (with trailing s).

2. CREATE INDEX idx_ai_usage_log_tenant_date ON public.ai_usage_log (tenant_id, created_at DESC);
   CREATE INDEX idx_ai_usage_log_feature ON public.ai_usage_log (feature);

3. ALTER TABLE public.ai_usage_log ENABLE ROW LEVEL SECURITY;

4. RLS policy — tenant read access:
   CREATE POLICY "ai_usage_log_select_tenant"
     ON public.ai_usage_log FOR SELECT TO authenticated
     USING (public.has_tenant_access(auth.uid(), tenant_id));
   No INSERT policy for authenticated (inserts are service_role only via SECURITY DEFINER RPC).

5. ALTER TABLE public.subscription_tiers ADD COLUMN IF NOT EXISTS ai_monthly_credits INT;
   UPDATE with locked budgets:
   - trial → 100
   - sandbox → 0
   - architect_office → 0
   - architect_office_ai → 500
   - construction → 0
   - construction_ai → 2000
   - enterprise → NULL (unlimited)

6. ALTER TABLE public.tenants ADD COLUMN IF NOT EXISTS ai_credits_purchased INT NOT NULL DEFAULT 0;

7. CREATE OR REPLACE FUNCTION public.add_ai_credits(p_tenant_id UUID, p_credits INT) RETURNS VOID
   LANGUAGE sql SECURITY DEFINER AS $$
     UPDATE public.tenants SET ai_credits_purchased = ai_credits_purchased + p_credits WHERE id = p_tenant_id;
   $$;

8. CREATE OR REPLACE FUNCTION public.consume_ai_actions(
     p_tenant_id UUID, p_feature TEXT, p_actions INT, p_user_id UUID, p_model_used TEXT,
     p_tokens_in INT DEFAULT 0, p_tokens_out INT DEFAULT 0, p_cost_brl DECIMAL DEFAULT 0, p_cached BOOLEAN DEFAULT FALSE
   ) RETURNS JSONB LANGUAGE plpgsql SECURITY DEFINER AS $$
   DECLARE
     v_tier_id TEXT;
     v_monthly_budget INT;
     v_credits_purchased INT;
     v_effective_budget INT;
     v_used_this_month INT;
     v_remaining INT;
     v_degraded BOOLEAN := FALSE;
   BEGIN
     SELECT subscription_tier_id, ai_credits_purchased
     INTO v_tier_id, v_credits_purchased
     FROM public.tenants WHERE id = p_tenant_id FOR UPDATE;

     IF v_tier_id = 'enterprise' THEN
       INSERT INTO public.ai_usage_log (tenant_id, user_id, feature, actions_consumed, model_used,
         actual_tokens_in, actual_tokens_out, actual_cost_brl, cached)
       VALUES (p_tenant_id, p_user_id, p_feature, p_actions, p_model_used,
         p_tokens_in, p_tokens_out, p_cost_brl, p_cached);
       RETURN jsonb_build_object('allowed', TRUE, 'degraded', FALSE, 'remaining', 999999);
     END IF;

     SELECT ai_monthly_credits INTO v_monthly_budget
     FROM public.subscription_tiers WHERE id = v_tier_id;

     v_effective_budget := COALESCE(v_monthly_budget, 0) + COALESCE(v_credits_purchased, 0);

     SELECT COALESCE(SUM(actions_consumed), 0) INTO v_used_this_month
     FROM public.ai_usage_log
     WHERE tenant_id = p_tenant_id AND created_at >= date_trunc('month', NOW());

     v_remaining := v_effective_budget - v_used_this_month;

     IF v_remaining <= 0 THEN
       v_degraded := TRUE;
     END IF;

     INSERT INTO public.ai_usage_log (tenant_id, user_id, feature, actions_consumed, model_used,
       actual_tokens_in, actual_tokens_out, actual_cost_brl, cached)
     VALUES (p_tenant_id, p_user_id, p_feature, p_actions, p_model_used,
       p_tokens_in, p_tokens_out, p_cost_brl, p_cached);

     RETURN jsonb_build_object(
       'allowed', TRUE,
       'degraded', v_degraded,
       'remaining', GREATEST(v_remaining - p_actions, 0)
     );
   END;
   $$;

Note: 'allowed' is always TRUE — 100% exhaustion degrades silently, never blocks (per CONTEXT.md locked decision). The 90% threshold banner is a UI concern only, not enforced at the DB level.
  </action>
  <verify>
    <automated>npm run validate:json 2>/dev/null; echo "Migration file created:"; ls /Users/amacedo/github/CastorWorks-NG/supabase/migrations/20260307000000_ai_usage_log_and_credits.sql && echo "OK"</automated>
  </verify>
  <done>
    Migration file exists with: ai_usage_log table, RLS policy, two indexes, ai_monthly_credits column with seeded values, ai_credits_purchased column on tenants, add_ai_credits RPC, and consume_ai_actions RPC. File lints cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 test scaffolds — useAIUsage, AIUsagePage, and ai-metering Deno tests</name>
  <files>
    src/hooks/__tests__/useAIUsage.test.ts,
    src/components/Settings/__tests__/AIUsagePage.test.tsx,
    supabase/functions/__tests__/ai-metering.test.ts
  </files>
  <action>
Create three test scaffold files that define the expected behaviors BEFORE implementations exist. Tests must fail (RED) because the source files don't exist yet. This is the Wave 0 requirement from 07-VALIDATION.md.

**File 1: src/hooks/__tests__/useAIUsage.test.ts**

Use Vitest. Mock `@/integrations/supabase/client` and `@/contexts/TenantContext`. Scaffold tests for:
- AI-01: ai_usage_log row contains tenant_id, feature, actions_consumed (verify query includes these columns)
- AI-01: Feature breakdown aggregation returns correct top-5 counts from mock log data
- AI-02: useAIUsage computes effectiveBudget as ai_monthly_credits + ai_credits_purchased
- AI-02: Enterprise tier (ai_monthly_credits = null) shows unlimited (effectiveBudget = null or Infinity)
- AI-03: consumeAIActions-equivalent: when remaining > 0, result has degraded = false
- AI-03: consumeAIActions-equivalent: when remaining <= 0, result has degraded = true

Import path: `import { useAIUsage } from '@/hooks/useAIUsage'`

**File 2: src/components/Settings/__tests__/AIUsagePage.test.tsx**

Use Vitest + React Testing Library (already in project). Mock `@/hooks/useAIUsage`. Scaffold tests for:
- AI-04: AIUsagePage renders progress bar with "X / Y AI Actions used this month"
- AI-04: At 80% usage, badge is shown on/near the component (no app-wide banner)
- AI-04: At 100% usage, inline nudge renders ("Running on reduced AI"); no blocking modal
- AI-04: When isEnterprise = true, no badge and no nudge renders

Import path: `import { AIUsagePage } from '@/components/Settings/AIUsagePage'`

**File 3: supabase/functions/__tests__/ai-metering.test.ts**

Use Deno test syntax (reference: supabase/functions/__tests__/analyze-site-photos.test.ts for the exact pattern). Mock the Supabase RPC call. Scaffold tests for:
- AI-04: When consumeAIActions mock returns { degraded: true }, the EF would pass preferredProvider='openrouter'
- AI-04: When consumeAIActions mock returns { degraded: false }, preferredProvider is undefined/default

Use TEST_MODE=stub pattern consistent with the existing Deno test file in that directory. The actual import path will be: `import { consumeAIActions } from '../_shared/ai-metering.ts'` — this import will fail until Plan 02 creates the file, which is expected (RED state).

All three files must be syntactically valid but have failing tests because source files are absent.
  </action>
  <verify>
    <automated>npm run test:run -- src/hooks/__tests__/useAIUsage.test.ts src/components/Settings/__tests__/AIUsagePage.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    Three test scaffold files exist and are syntactically valid. Vitest tests fail with import/module errors (expected RED state — source files don't exist yet). Deno test file exists at supabase/functions/__tests__/ai-metering.test.ts.
  </done>
</task>

</tasks>

<verification>
- supabase/migrations/20260307000000_ai_usage_log_and_credits.sql exists and contains consume_ai_actions, add_ai_credits, ai_usage_log, ai_monthly_credits, ai_credits_purchased
- npm run lint passes (SQL file not linted; check no TypeScript errors in test files)
- Three test scaffold files exist at correct paths
- npm run test:run on the two Vitest scaffolds shows failing tests (not syntax errors)
</verification>

<success_criteria>
- Migration file is complete and syntactically correct SQL
- ai_usage_log table has RLS enabled with authenticated-read policy
- consume_ai_actions returns { allowed: true, degraded: false, remaining: 999999 } for Enterprise (no budget check)
- consume_ai_actions returns { allowed: true, degraded: true } when v_remaining <= 0 (silent degradation, never blocks)
- add_ai_credits atomically increments ai_credits_purchased
- All three Wave 0 test scaffold files exist with meaningful test descriptions (failing is correct)
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-action-credits-metering/07-01-SUMMARY.md`
</output>
